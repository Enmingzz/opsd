diff --git a/Qwen2_5_VL/qwen2_5vl_visionzip.py b/Qwen2_5_VL/qwen2_5vl_visionzip.py
index 3255982..33fec49 100644
--- a/Qwen2_5_VL/qwen2_5vl_visionzip.py
+++ b/Qwen2_5_VL/qwen2_5vl_visionzip.py
@@ -307,7 +307,6 @@ class Qwen2_5_VLVisionAttention(nn.Module):
 
 
 class Qwen2_5_VLVisionSdpaAttention(nn.Module):
-    ## Note: Current implementation is not support the SpdaAttention
     def __init__(self, dim: int, num_heads: int = 16) -> None:
         super().__init__()
         self.num_heads = num_heads
@@ -320,6 +319,7 @@ class Qwen2_5_VLVisionSdpaAttention(nn.Module):
         cu_seqlens: torch.Tensor,
         rotary_pos_emb: Optional[torch.Tensor] = None,
         position_embeddings: Optional[Tuple[torch.Tensor, torch.Tensor]] = None,
+        return_logits: bool = False,
     ) -> torch.Tensor:
         seq_length = hidden_states.shape[0]
         q, k, v = self.qkv(hidden_states).reshape(seq_length, 3, self.num_heads, -1).permute(1, 0, 2, 3).unbind(0)
@@ -347,7 +347,17 @@ class Qwen2_5_VLVisionSdpaAttention(nn.Module):
         attn_output = attn_output.transpose(0, 1)
         attn_output = attn_output.reshape(seq_length, -1)
         attn_output = self.proj(attn_output)
-        return attn_output
+
+        if return_logits:
+            with torch.no_grad():
+                attn_logits = torch.matmul(q, k.transpose(1, 2)) / (q.shape[-1] ** 0.5)
+                attn_logits = attn_logits.masked_fill(~attention_mask, torch.finfo(attn_logits.dtype).min)
+                attn_logits = F.softmax(attn_logits, dim=-1, dtype=torch.float32).to(q.dtype)
+                return_k = k
+        else:
+            attn_logits = None
+            return_k = None
+        return attn_output, attn_logits, return_k
 
 
 QWEN2_5_VL_VISION_ATTENTION_CLASSES = {
@@ -1913,8 +1923,14 @@ class Qwen2_5_VLForConditionalGeneration(Qwen2_5_VLPreTrainedModel, GenerationMi
                 position_ids = position_ids.unsqueeze(0).expand(3, -1, -1)
         if select_pixel:
             
-            dominant_num = int(0.65 * attn_logits.size(0))
-            contextual_num = max(int(0.05 * attn_logits.size(0)),1)
+            num_visual_tokens = int(attn_logits.size(0))
+            dominant_ratio = float(getattr(self, 'visionzip_dominant_ratio', 0.65))
+            contextual_ratio = float(getattr(self, 'visionzip_contextual_ratio', 0.05))
+            dominant_num = min(num_visual_tokens, max(0, int(dominant_ratio * num_visual_tokens)))
+            contextual_num = max(0, int(contextual_ratio * num_visual_tokens))
+            if contextual_ratio > 0 and dominant_num < num_visual_tokens:
+                contextual_num = max(contextual_num, 1)
+            contextual_num = min(contextual_num, max(0, num_visual_tokens - dominant_num))
             topk_values, topk_indices = torch.topk(attn_logits, dominant_num)
 
             mask = torch.zeros_like(attn_logits, dtype=torch.bool)
@@ -1965,6 +1981,12 @@ class Qwen2_5_VLForConditionalGeneration(Qwen2_5_VLPreTrainedModel, GenerationMi
             attention_mask = attention_mask[:, img_mask]
             inputs_embeds[:,contexual_input_idx] =  contextual_tokens
             inputs_embeds = inputs_embeds[:, img_mask]
+            self._last_visionzip_pruned_inputs = {
+                'input_ids': input_ids[:, img_mask].detach(),
+                'inputs_embeds': inputs_embeds.detach(),
+                'attention_mask': attention_mask.detach() if attention_mask is not None else None,
+                'position_ids': position_ids.detach() if position_ids is not None else None,
+            }
             del contextual_tokens, hidden_states_filtered, hidden_to_merge,aggregated_hidden
 
 
